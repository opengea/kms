
function loadWidgets() {

  $.ajax --> kms_widgets (userid)


}

//function createWidget (name, tipus, width, height, x, y, zindex, ..)

function refreshWidgetView (widget_id, view_id) {

  $.ajax -->

        $widgets[0]['views'][0] = array();
        $widgets[0]['views'][0]['name'] = "totes";
        $widgets[0]['views'][0]['dr_folder'] = '%';
        $widgets[0]['views'][0]['filter'] = "(status='pendent' or status='en_proces' or status='espera')";
        $widgets[0]['views'][0]['orderby'] = "priority desc";
        $widgets[0]['views'][0]['mod']="tasks";

   $('#'+widgets[wiload (

}
